package ${package}.${moduleName}.controller;

import ${mainPath}.core.bean.BaseController;
import ${mainPath}.${moduleName}.mapper.${className}Mapper;
import ${mainPath}.${moduleName}.po.${className}Entity;
import ${mainPath}.${moduleName}.service.impl.${className}ServiceImpl;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@RestController
@RequestMapping("${moduleName}/${pathName}")
public class ${className}Controller extends BaseController<${className}ServiceImpl, ${className}Mapper,${className}Entity>{

}
